import SwiftUI

// MARK: - Sci-Fi Color Palette
struct SciFi {
    static let bgDeep      = Color(red: 0.04, green: 0.04, blue: 0.10)
    static let bgPanel     = Color(red: 0.08, green: 0.08, blue: 0.16)
    static let bgCard      = Color(red: 0.10, green: 0.10, blue: 0.20)
    static let bgRow       = Color(red: 0.07, green: 0.07, blue: 0.14)
    static let neonCyan    = Color(red: 0.0, green: 0.9, blue: 1.0)
    static let neonMagenta = Color(red: 1.0, green: 0.0, blue: 0.6)
    static let neonGreen   = Color(red: 0.0, green: 1.0, blue: 0.5)
    static let neonPurple  = Color(red: 0.6, green: 0.2, blue: 1.0)
    static let neonOrange  = Color(red: 1.0, green: 0.6, blue: 0.0)
    static let textPrimary = Color.white
    static let textDim     = Color(white: 0.55)
    static let border      = Color.white.opacity(0.08)
}
